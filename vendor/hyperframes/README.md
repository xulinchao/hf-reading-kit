# 官方 HyperFrames Blocks

在本目录执行安装：

```bash
cd I:\Project\hf-reading-kit
npx hyperframes add data-chart --dir vendor/hyperframes
npx hyperframes add lt-dark-card --dir vendor/hyperframes
```

安装后更新 `../mapping.yaml` 的 `vendor_hyperframes` 路径。

浏览全部：`npx hyperframes catalog --type block`
